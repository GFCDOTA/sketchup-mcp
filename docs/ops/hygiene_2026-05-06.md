# Repo hygiene cycle — 2026-05-06

First hygiene pass under §15 of `CLAUDE.md` (Repository Hygiene Protocol).

## Removed

| Path | Size | Status before | Reason |
|---|---|---|---|
| `sketchup-mcp.rar` | 41 MB | untracked (gitignored implicitly via no track) | local backup archive; not referenced by any code/doc/CI; deleted via `rm` (no commit needed since untracked) |

## Local + remote branches deleted (post-merge cleanup per §0)

All five had been merged into `develop` via PRs #44 / #45 / #46 / #47 / #48 earlier the same day.

- `feature/openings-as-components` (was `4432dd4`)
- `feature/openings-room-context-classifier` (was `ac699e0`)
- `feature/coherence-audit` (was `fdea6f1`)
- `feature/plan-truth-gate-planta-74` (was `9dd7eb6`)
- `feature/micro-truth-gate-planta-74` (was `f25b0bf`)

Both local refs and remote tracking refs removed.

## Preserved (with rationale)

| Suspect | Why preserved |
|---|---|
| `proto_red.py`, `proto_colored.py`, `proto_skel.py`, `proto_v2.py`, `proto_runner.py`, `peek_pdf.py`, `crop_legend.py`, `analyze_overpoly.py`, `preprocess_walls.py`, `make_test_pdf.py` | Referenced by `docs/ROADMAP.md`, `docs/repo_hardening_plan.md`, `pyproject.toml`, and historical archive notes. Per §15, anything referenced by docs is preserved. |
| `PROMPT-FELIPE.md`, `PROMPT-RENAN.md` | Referenced by `patches/README.md` + historical archive. Historical onboarding artifacts; classified as `historical baseline`, kept. |
| `OVERVIEW.md`, `AGENTS.md` | Active operational docs referenced from CLAUDE.md and agent definitions. |
| `docs/SOLUTION.md` | Referenced by 9 files including `OVERVIEW.md` and agent docs. Active. |
| `docs/openings_vector_v0.md` | Searched: no inbound references in code, but a historical algorithm doc still useful for diff with current vector pipeline. Classified `historical baseline`, kept (in-doc rule: prefer preserve when in doubt). |
| All `runs/<dir>/` (36 directories) | All under gitignored `/runs/` path — not in version control. Local artifacts only. Per §15 protected-paths rule: `runs/` is in the §1 list of paths that require explicit human approval to delete. No action. |
| `reports/repo_audit*.{json,md}` and `reports/perf_baseline.json` | Match `reports/.gitignore` patterns; not in version control. No action. |
| `dashboard/architecture-sre-radar`, `dashboard/project-roadmap` branches | Active dashboard work, unrelated to merged feature stack. Preserve. |

## Reference searches performed

- `grep` for `sketchup-mcp.rar` across whole repo → no matches.
- `grep` for `proto_red|proto_colored|proto_skel|proto_v2|proto_runner|peek_pdf|crop_legend|analyze_overpoly|preprocess_walls|make_test_pdf` → 8 doc references.
- `grep` for `PROMPT-FELIPE|PROMPT-RENAN` → 5 references.
- `grep` for `openings_vector_v0|SOLUTION.md` → 9 references.
- `grep` for `runs/<each-old-dir>` → 8 references in code, but for legacy `runs/baseline_2026-04-29` etc. inside test fixtures or doc archive only; gitignored anyway.
- `git ls-files reports/` → only `.gitignore`, `.gitkeep`, `perf_baseline.example.json` tracked.
- `cat .gitignore` + `cat reports/.gitignore` → confirmed `/runs/` and `repo_audit*` excluded.

## Validations

- `git status` after deletes → clean working tree (no unintended file changes).
- `git branch -a` → no orphaned local refs to deleted feature branches.
- No code, no schema, no algorithmic change in this cycle. Pytest / smoke not re-run because no source touched (beyond the .rar removal which is untracked).

## What this cycle did NOT do

- No deletion inside `runs/` (protected by §1 hard rule + gitignored).
- No deletion of `proto_*.py` (referenced by current docs).
- No deletion of any `.md` (all classified as active or historical baseline).
- No mixing of cleanup with algorithmic change (separate branch `chore/repo-hygiene-2026-05-06`; the only file touch is this report).

## Next hygiene candidates (deferred — need deeper analysis)

- Audit `runs/` after a rule §1 amendment: 36 dirs accumulated; most look obsolete (e.g., `post_aggressive_filter`, `post_combined`, `post_thin_v2`, `v1_pipeline_before/after`). Should be moved to `runs/_archive/` if classified safe.
- Consider dropping legacy `proto_*.py` once the raster pipeline is officially retired (currently §10 notes raster as OUTDATED but kept).
- `docs/_archive/2026-04-f1-cycle/` already follows the archive pattern — model for any future doc archives.

## Summary

- 1 file removed (41 MB local backup, untracked).
- 5 local + 5 remote feature branches deleted (post-merge cleanup).
- 0 tracked file deletions.
- 0 risky changes.
- Working tree clean.
