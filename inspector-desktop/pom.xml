<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>br.dev.felipe.inspector</groupId>
  <artifactId>inspector-desktop</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>AI Pipeline Inspector — desktop</name>
  <description>
    Host desktop do AI Pipeline Inspector (ADR-001). Java = domínio + I/O;
    React = visualização. Cliente do trace do sketchup-mcp; NÃO é um segundo backend.
  </description>

  <properties>
    <maven.compiler.release>25</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <javafx.version>25.0.4</javafx.version>
    <jackson.version>2.18.2</jackson.version>
    <junit.version>5.11.4</junit.version>
    <main.class>inspector.ui.InspectorApp</main.class>
    <!-- smoke check nao-interativo: ./mvnw javafx:run -Dselftest=true -->
    <selftest>false</selftest>
    <!-- trace alternativo; vazio = o .jsonl mais recente de ../.ai_bridge/traces -->
    <trace/>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-controls</artifactId>
      <version>${javafx.version}</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-web</artifactId>
      <version>${javafx.version}</version>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>${jackson.version}</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.2</version>
      </plugin>
      <plugin>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-maven-plugin</artifactId>
        <version>0.0.8</version>
        <configuration>
          <mainClass>${main.class}</mainClass>
          <options>
            <option>--enable-native-access=javafx.graphics,javafx.web</option>
            <option>-Dselftest=${selftest}</option>
            <option>-Dtrace=${trace}</option>
          </options>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
