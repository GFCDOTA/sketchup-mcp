# Visual-confirm pendente: planta_74 fidelity_4axis_report.json

## Context

PR #121 (`feat(human-walls): protocol + tools to fix global SKP
visual fidelity`) merged with the following 4-axis verdict on
`fixtures/planta_74/fidelity_4axis_report.json`:

| Axis                     | Verdict | Notes                                                  |
|--------------------------|---------|--------------------------------------------------------|
| `wall_fidelity`          | PASS    | `h_o005` cut_into_wall via host `h_w000`               |
| `soft_barrier_fidelity`  | PASS    | 0 cells need a soft_barrier after the 2026-05-13 prior flip |
| `semantic_room_fidelity` | PASS    | SALA DE JANTAR \| SALA DE ESTAR labels preserved       |
| `global_visual_fidelity` | **WARN**| **operator waived visual review — see acknowledgment** |

Top-level verdict: **WARN** (advisory only; merge unblocked by
the 3 hard axes).

The operator's 2026-05-13 acknowledgment is preserved at
[`fixtures/planta_74/operator_acknowledgment_2026-05-13.md`](../fixtures/planta_74/operator_acknowledgment_2026-05-13.md).

## What this issue tracks

Close the advisory loop in the next planta_74 cycle: have someone
(operator OR an LLM via the GPT-bridge or the local Ollama
endpoint, per the autonomy-protocol memory) visually compare the
side_by_side render against the source PDF, then either flip the
verdict or file a concrete drift report.

## Artifacts to compare

- **Side-by-side**: [`fixtures/planta_74/side_by_side_pdf_vs_skp_FINAL.png`](../fixtures/planta_74/side_by_side_pdf_vs_skp_FINAL.png)
  (708 KB) — PDF on the left, SKP axon on the right.
- **Source PDF**: `planta_74.pdf` (not in git, fetched separately).
- **Door audit**: [`fixtures/planta_74/door_audit_FINAL.png`](../fixtures/planta_74/door_audit_FINAL.png)
- **Fidelity overlay**: [`fixtures/planta_74/fidelity_FINAL.png`](../fixtures/planta_74/fidelity_FINAL.png)
- **Render notes**: [`fixtures/planta_74/notes_FINAL.md`](../fixtures/planta_74/notes_FINAL.md)
- **Diagnostic dump used to confirm the prior change**:
  [`fixtures/planta_74/claude_diagnostic_for_user_validation.png`](../fixtures/planta_74/claude_diagnostic_for_user_validation.png)
  (generated by `tools/render_diagnostic_for_user.py`).

## Definition of done

One of:

1. **PASS** — Visual reviewer (operator or LLM) confirms the
   side-by-side is faithful enough for furniture/layout planning.
   Re-run:
   ```
   python -m tools.verify_fidelities \
     --consensus runs/vector/consensus_with_human_walls_and_soft_barriers.json \
     --candidates fixtures/planta_74/loop_closure_candidates_after_soft_barriers.json \
     --side-by-side fixtures/planta_74/side_by_side_pdf_vs_skp_FINAL.png \
     --out fixtures/planta_74/fidelity_4axis_report.json \
     --operator-confirmed-visual
   ```
   commit + push the updated `fidelity_4axis_report.json`
   (top-level should flip to PASS).
2. **WARN-with-drift** — Reviewer flags specific geometric drift.
   File a follow-up issue per drift, link it from this issue, and
   leave the verdict as WARN. Do NOT pass the flag.

## Visual-checklist (5 binary questions used in the 2026-05-13 cycle)

1. Outer envelope of SKP matches the PDF's "inverted-L" shape?
2. SUITE 01, SUITE 02, BANHO 01, BANHO 02, LAVABO, COZINHA are
   visually identifiable as separate cells in the SKP axon?
3. A.S. / TERRACO SOCIAL / TERRACO TECNICO render as a single
   continuous space, with the outer parapet (`h_sb000`) hugging
   the south facade only?
4. SALA renders as one unified cell (jantar + estar) behind the
   COZINHA?
5. No floating cells, wall-through-wall pathologies, or broken
   geometry?

5×yes → safe to pass the flag.
any no → file the drift detail in this issue's thread, do NOT
pass the flag.

## Why this is not a blocker

`global_visual_fidelity` is **advisory** per the verify_fidelities
contract (`tools/verify_fidelities.py:51–55`). The three hard
axes all PASS. The substantive change in PR #121 was
**semantic** (prior flip, not geometric), and the render is a
verification artifact, not a deliverable.

## Suggested next session

Bundle this with the next planta_74 work (e.g., when Slice 6a's
dogfood needs a real consensus rerun) so the visual review is
amortised, not a standalone cycle.
