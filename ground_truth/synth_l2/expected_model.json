{
  "schema_version": "1.0",
  "plan_id": "synth_l2",
  "source_pdf": "(generated by tools.synth.make_synthetic_vector_pdf --spec l2)",
  "unit": "m",
  "scale_source": "synthetic — PDF user space units treated as PT_TO_M=0.0352 like planta_74; not physically anchored",
  "global_bbox": {
    "width": 17.6,
    "height": 7.0,
    "tolerance_pct": 25
  },
  "expected_counts": {
    "rooms": 2,
    "openings": 1,
    "walls": 9,
    "tolerance": {
      "rooms_delta": 0,
      "openings_delta": 1,
      "walls_delta": 2
    }
  },
  "rooms": [
    {
      "id": "sala_synth",
      "label": "SALA SYNTH",
      "expected_area_m2_range": [3.0, 18.0],
      "must_be_closed": true,
      "manual_confidence": "high",
      "source": "synthetic — left-lower L-arm of EXAMPLE_SPEC_2_ROOM_L",
      "notes": [
        "Wide range: synth's exact area depends on canonicalization",
        "tolerance and the precise wall-thickness clustering. The point",
        "is round-trip ingestability, not metric pinning."
      ]
    },
    {
      "id": "suite_synth",
      "label": "SUITE SYNTH",
      "expected_area_m2_range": [25.0, 60.0],
      "must_be_closed": true,
      "manual_confidence": "high",
      "source": "synthetic — right wing of EXAMPLE_SPEC_2_ROOM_L",
      "notes": [
        "Larger by design (the L's long arm). Wide range for the same",
        "reason as SALA SYNTH."
      ]
    }
  ],
  "openings": [
    {
      "id": "op_sala_suite_passage",
      "kind": "interior_passage",
      "connects": ["sala_synth", "suite_synth"],
      "manual_confidence": "high",
      "notes": [
        "Wall-gap on the divider between the two rooms; widened to",
        "50 pt in Cycle 11d so DEFAULT_GAP_MIN_PTS=30 picks it up."
      ]
    }
  ],
  "adjacency": [
    {
      "a": "sala_synth",
      "b": "suite_synth",
      "via": "op_sala_suite_passage",
      "kind": "interior_passage",
      "manual_confidence": "high"
    }
  ],
  "notes": [
    "Cycle 11c (2026-05-08) — synthetic ground truth paired with",
    "tools.synth.make_synthetic_vector_pdf. The PDF is a 2-room",
    "L-shape generated as filled-rectangle walls + Helvetica labels.",
    "Goal: round-trip the REAL extraction pipeline against a known-",
    "intent input, complementing tools.fidelity.synth_from_expected",
    "(which round-trips the engine internally only).",
    "Cycle 11d (2026-05-08): wall-gap widened to 50 pt so the",
    "wall-gap detector emits 1 interior_passage opening. The full",
    "round-trip now exercises wall + room + opening + classifier",
    "+ fidelity stages — not just walls + rooms."
  ]
}
